# 03 Router DDNS Tests And Docs

Status: `in-progress`

Suggested branch: `docs/router-ddns-tests-and-docs`

## Goal

Add the minimal docs and checks needed so consumers can adopt the DDNS module
with confidence once it exists.

## Scope

- add README/module docs for DDNS
- add at least one evaluation-oriented check path
- document secret-file expectations and public DNS scope

## Validation

- DDNS usage is discoverable from the repo root
- at least one CI-visible path exercises the module
