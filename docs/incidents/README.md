# Incident Documentation

This directory holds repo-tracked incident records.

When a regression is large enough that multiple agents may probe it in
parallel, use the workspace orchestration standard under:

- `/home/deepwatrcreatur/flakes/standards/agentic-orchestration/`

Minimum expectations:

1. read `SUMMARY.md` first
2. log probes in `RESEARCH_LEDGER.md`
3. keep signed reasoning in `ACTIVE_DISCUSSION.md`
4. do not mark an incident resolved without a repo-tracked evidence trail
